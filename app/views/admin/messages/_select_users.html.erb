        <%= search_form_for @q, url: admin_users_path do |f| %>
        <div class="form-group row h5 justify-content-center">
          <div class="col-sm-2">
            <%= f.label :company_name, "宛先" %>
          </div>
          <div class="col-sm-4">
            <%= f.collection_select :company_name, User.all, :id, :company_name ,class: "form-control", style: "width:80%" %>
          </div>
        </div>

        <% end %>